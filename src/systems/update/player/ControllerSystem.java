package systems.update.player;

/**
 * A system for determining which entity the inputter (e.g., KeyHandler) is bound to.
 *
 * <p></> Updates the Game entity's component to translate which entity is being controlled.
 */
public class ControllerSystem {
    // Listens to SwapControls command which records "from" and "to"
}
